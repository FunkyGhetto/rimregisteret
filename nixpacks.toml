# Reassemble split db files before starting the app
[phases.assemble]
cmds = ['cat data/db/rimindeks.db.part* > data/db/rimindeks.db']

[phases.install]
dependsOn = ['assemble']

[start]
cmd = 'uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000}'
