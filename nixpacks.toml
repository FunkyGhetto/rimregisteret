[phases.setup]
nixPkgs = ['git-lfs']

[phases.lfs]
dependsOn = ['setup']
cmds = ['git lfs install --skip-repo', 'git lfs pull || echo "LFS pull failed, checking if files exist..."']

[phases.install]
dependsOn = ['lfs']

[start]
cmd = 'uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000}'
